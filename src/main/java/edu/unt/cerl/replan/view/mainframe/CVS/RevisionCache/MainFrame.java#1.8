package edu.unt.cerl.replan.view.mainframe;

import edu.unt.cerl.replan.controller.action.MainFrameWindowListener;
import java.awt.Color;
import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JPanel;
import org.jdesktop.layout.GroupLayout;

/**
 * Class MainFrame
 */
public class MainFrame extends JFrame {

    //
    // Fields
    //
    //private JTabbedPane scenarioPanes;
    private ScenarioTabbedPane tabs;
    private REPLANMenuBar menuBar;

    //
    // Constructors
    //
    /**
     * Creates the main frame of the RE-PLAN framework
     */
    public MainFrame() {
        // Initialize view components of the main frame
        this.setSize(1000, 800);

        // init();

        this.setLocationRelativeTo(null);

        this.initComponents();
        this.setVisible(true);

        this.invalidate();
        this.repaint();
        this.setVisible(true);



    }

    //    //
    // Accessor methods
    //
    //
    // Other methods
    //
    // Methods
    //

    /*
     * This method is called by the constructor and initializes the view
     * components of the main frame
     */
    private void initComponents() {

        menuBar = new REPLANMenuBar(this);

        tabs = new ScenarioTabbedPane(); //menuBar);

        this.addWindowListener(new MainFrameWindowListener(this));
        setDefaultCloseOperation(javax.swing.WindowConstants.DO_NOTHING_ON_CLOSE);
        //setTitle("RE-PLAN: REsponse PLan ANalyzer");
        setTitle("RE-PLAN Bio-E"); // We may want to parameterize this title based on the functionality we are loading into the system with the XML settings file

        setJMenuBar(menuBar);
        this.getContentPane().setBackground(Color.yellow);

        GroupLayout layout = new GroupLayout(getContentPane());
        getContentPane().setLayout(layout);


        this.setContentPane(tabs);

    }



    //
    // Accessor methods
    //
    //
    // Other methods
    //
    /**
     */
    public void disableActionsDuringReplan() {
    }

    /**
     */
    public void enableActionsAfterReplan_() {
    }

    public ScenarioTabbedPane getTabs() {
        return this.tabs;
    }

    public REPLANMenuBar getREPLANMenuBar() {
        return this.menuBar;
    }
}
